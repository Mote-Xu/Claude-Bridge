{
  "name": "claude-bridge-agent",
  "version": "1.0.0",
  "description": "Windows local agent for Claude Bridge — replaces SSH with HTTP",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
